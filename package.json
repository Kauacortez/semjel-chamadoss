{
  "name": "semjel-chamados-root",
  "version": "2.0.0",
  "private": true,
  "description": "Root package.json for Vercel deploy compatibility",
  "scripts": {
    "build": "echo 'Building root...'"
  }
}
